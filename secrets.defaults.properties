# DO NOT PUT REAL SECRETS HERE.
# Developers set real values in local.properties (not committed).
#
# AI SPLIT (important):
#   Firebase AI Logic (Gemini) does NOT use a raw API key at Android runtime.
#   Authentication is handled via google-services.json + google-services plugin.
#   See docs/SECRETS.md for Firebase setup instructions.
#
#   GOOGLE_MAPS_API_KEY = Android map display + scan history heatmap
#
# Coding agents (Claude, Codex, Copilot) do NOT use these keys.
# See .env.example for desktop/dev environment keys.

GOOGLE_MAPS_API_KEY=REPLACE_ME
