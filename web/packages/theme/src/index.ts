/**
 * @wscanplus/theme — shared design tokens for desktop and web UIs.
 *
 * Source of truth: design/tokens/wscan.tokens.json (regenerated via scripts/generate).
 *
 * Usage:
 *   import { color, space, threatColor, rssiBucket } from '@wscanplus/theme';
 *   import '@wscanplus/theme/tokens.css';  // applies CSS vars on :root
 */

export { color, radius, space, motion, font, threatColor, threatShort, rssiBucket } from './tokens.js';
export type { ThreatLevel } from './tokens.js';
