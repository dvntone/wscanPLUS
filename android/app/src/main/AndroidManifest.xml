<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ================================================================
         WiFi scanning — required across all supported API levels (24–36)
         ================================================================ -->

    <!-- Normal permissions — granted at install time -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!--
        Dangerous permissions — must be requested at runtime (API 24+).
        Android 12+ requires both FINE and COARSE to be declared; the user
        may choose to grant only COARSE — app must handle that gracefully.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!--
        NEARBY_WIFI_DEVICES (API 33+): required for WiFi P2P, WiFi Aware, and hotspot
        APIs on API 33+. Does NOT replace ACCESS_FINE_LOCATION for getScanResults() —
        that method was explicitly excluded from the NEARBY_WIFI_DEVICES migration and
        requires ACCESS_FINE_LOCATION on all API levels. neverForLocation ensures this
        permission is not used to derive location from WiFi data.
    -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="33" />

    <!-- ================================================================
         WatchdogService foreground service permissions
         ================================================================ -->

    <!-- Required API 28+ to call startForeground() -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!--
        Required API 34+ for foregroundServiceType="dataSync".
        Guards the 6-hour dataSync runtime limit on Android 15.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Required for ServerSocket(9000) ADB communication (Phase 3) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ================================================================
         BLE readiness and NMEA tethering support
         ================================================================ -->

    <!-- Legacy Bluetooth (API ≤30) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Runtime permissions required API 31+ for BLE readiness, GATT server, and advertising -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" tools:targetApi="31" />

    <!-- Required for CameraIrSelfTest (user-triggered capability self-test only) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Required for AcousticSelfTest (user-triggered capability self-test only) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Required API 33+ to post notifications (e.g. permission-revoked alert from WatchdogService) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ================================================================
         Biometric authentication
         HARD RULE: NEVER remove USE_BIOMETRIC — silent crash on Android 14+
         ================================================================ -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="false"
        android:name=".WscanApp"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="wscan+"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".KismetSettingsActivity"
            android:exported="false" />

        <activity
            android:name=".ScanMapActivity"
            android:exported="false" />

        <activity
            android:name=".ThreatResultsActivity"
            android:exported="false" />

        <activity
            android:name=".ScanHistoryActivity"
            android:exported="false" />

        <activity
            android:name=".DiagnosticActivity"
            android:exported="false" />

        <activity
            android:name=".theme.ThemeSettingsActivity"
            android:exported="false" />

        <!--
            FileProvider for sharing JSON exports from ScanDataExporter.
            Exposes cacheDir/exports/ only — no other paths.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
            WatchdogService — foreground service managing the scanner chain + ADB socket.
            foregroundServiceType includes location because Wi-Fi scan retrieval is
            location-gated on modern Android. dataSync remains for the planned sync path.
        -->
        <service
            android:name=".WatchdogService"
            android:foregroundServiceType="location|dataSync"
            android:exported="false" />

    </application>

</manifest>
